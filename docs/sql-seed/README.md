# Legacy SQL seed scripts

This folder preserves legacy SQL seed scripts previously used for local SQL Edge.

- These are not used by the app anymore after switching to Azure SQL with Azure AD Default.
- Keep for reference or one-off troubleshooting.
- Scripts were copied from dev-db/seed.
