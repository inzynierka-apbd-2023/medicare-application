# Main User Flows:
    - New User Registration Flow:

        Choose Plan ? Plan Selection ? Register ? Registration Success
    - Existing User Login Flow:

        Login ? Login Success ? Home (redirects automatically)
    - Password Recovery Flow:

        Login ? Forgot Password ? Password Reset Success ? Login
    - Card Number Recovery Flow:

        Login ? Forgot Card Number ? (SMS sent) ? Login